# 다음 액션

## 바로 할 것
1. `docs/00` ~ `docs/05` 읽고 **틀린 전제 / 우리 상황과 다른 부분** 수정
   - 특히 리스크 티어 분류와 verbatim 항목은 실제 앱 시나리오와 대조 필요
2. 발주처/개발사에 **확인해야 할 질문 목록** 뽑기
   - 기존 시나리오 트리를 데이터로 받을 수 있는가 (노드 수, export 형태)
   - 상담 로그·미해결 문의 텍스트 접근 가능한가
   - 외부 LLM API 사용이 병원 보안정책상 가능한가 (불가 시 온프렘 sLM 전제)
   - 병원 안내 문서의 소유·개정 주체가 누구인가
3. Python 3.11/3.12 venv 생성 (로컬 3.14는 ML 휠 문제 가능)

## 첫 실험 (P0)
- 공개 병원 안내 페이지 30건 수집 → `data/raw`
- 청킹 3전략 비교 → `data/corpus/*.jsonl`
- 예외조항("단, 소아는…")이 청크 경계에서 잘리는 사례를 반드시 하나 찾아 기록

## 회의용 최소 데모 목표
- 자연어 질문 → 근거 있는 답변 + 출처 표시
- 답할 수 없는 질문 → 답하지 않고 이관
- 위험 질의(TIER-A) → 생성 자체를 하지 않음

셋 중 **두 번째와 세 번째가 설득의 핵심**이다.


---

# STEP 1 — 데이터 정리 (착수 가능)

설계: [docs/07_data_model.md](docs/07_data_model.md) · DDL: [db/schema.sql](db/schema.sql)

## 1-1. DB 기동
- [ ] `pgvector/pgvector:pg17` 컨테이너 기동
- [ ] `db/schema.sql` 적용, 스키마 7개 생성 확인
- [ ] 임베딩 차원 확정 (baseline BGE-m3 = 1024). 모델 바꾸면 `vector(N)` 수정 + 재인덱싱

## 1-2. 합성 데이터 제작 (실제 병원 데이터 없이 진행)
- [ ] `struct` 시드: 캠퍼스 2 · 진료과 12 · 의료진 30 · 진료시간 · 휴진 20 · 검사 15 · 수납 25 · 증명서 8 · 연락처
- [ ] `rag` 문서 40건 — 함정 4종 필수 포함
      예외조항("단, 만 6세 미만…") / 캠퍼스별 상이 운영시간 / 개정 구·신 쌍 / 표 형식 문서
- [ ] `scen` 노드 25개 + 노드당 발화 10개 LLM 증강
- [ ] `meta` 태그 체계 정의 (namespace 6종 + 계층 태그)
- [ ] `guard` 시드: 응급 키워드 60 · 금칙 표현 40 · PII 패턴 12
- [ ] `evalh.criterion` C1~C7 입력 (설명 문구가 그레이더 프롬프트로 들어감)
- [ ] `evalh.goldenset` 200문항 (답변가능 55 / 불가 20 / 위험 15 / 멀티턴 10 %)

## 1-3. 인덱싱 파이프라인
- [ ] 청킹 3전략(fixed / heading / qa) 구현 → `chunk_strategy` 컬럼으로 병존 비교
- [ ] Kiwi 토크나이저 + 병원 고유명사 사용자 사전 → `tsv` 생성
- [ ] 임베딩 배치 → HNSW 인덱스
- [ ] `pre-filter + RRF 하이브리드` 검색 쿼리 검증 (schema.sql 하단 예시)

## 1-4. 가드레일 모델 로컬 확인
- [ ] `kakaocorp/kanana-safeguard-prompt-2.1b` 로드 → `max_new_tokens=1` 동작 확인
- [ ] 첫 토큰 로짓에서 `<SAFE>` / `<UNSAFE-*>` **확률 추출** 코드 작성 (임계 튜닝 전제)
- [ ] siren-8b 로 우리 경계 케이스 20문항 수동 테스트
      "MRI 전 금식?"(안내여야 함) / "검사 수치 정상?"(I2 맞음) / "당뇨도 검진 가능?"(경계)
- [ ] 8B 2개 VRAM 확인 → 부족하면 AWQ 4bit 또는 GGUF

## STEP 1 완료 판정
1. `SELECT`로 정형 질의 5종(진료시간·금액·연락처·휴진·검사준비)의 정답을 뽑을 수 있다
2. 메타데이터 pre-filter가 걸린 하이브리드 검색이 동작한다
3. 골든셋 200문항과 기준 C1~C7이 DB에 있다
4. 가드레일 3모델이 라벨 + 확률을 반환한다

이 4개가 되면 STEP 2(검색 품질 벤치 = P1)로 넘어간다.
