# 진행상황

## 2026-08-24
- 프로젝트 생성 (`hospital_rag_assistant`) — 병원 챗봇을 시나리오 단독 → 시나리오를 가드레일로 쓰는 RAG로 전환
- 기획·설계 문서 작성: `docs/00~11` (요구사항 / 아키텍처 / 로드맵 / 평가 / 정책 / 가드레일 / CRAG / 시퀀스 / 개발환경)
- DB 스키마 설계: `db/schema.sql` — Postgres+pgvector 하나에 스키마 7개 (struct/rag/meta/scen/guard/evalh/ops)
- 가드레일 = Kanana Safeguard 3종 채택 (Siren `I2` = 우리 TIER-A와 1:1, Apache-2.0 온프렘)
- CRAG 하네스 설계 — 기준 C1~C7 DB 저장, 실패유형→액션 매핑, 재검색 1라운드, 웹검색 폴백 제거
- Docker 스택 구성: `db` + `app` 2개 (+ `guardrail` GPU profile, 평소 안 뜸). compose 검증 통과
- 코드 0줄. 아직 `docker compose up` 안 함
- 다음: STEP 1 데이터 정리 → DB 기동·스키마 검증 → 합성 시드(struct/rag/scen) → 골든셋 200문항
- 확인 필요: 시나리오 트리 데이터 수령 가능? / 외부 LLM API 병원 보안정책상 허용?
- 미커밋

## 2026-08-25
- 깃 연습(복습) 커밋
