# 진행상황

## 2026-08-24
- 프로젝트 생성 (`hospital_rag_assistant`) — 병원 챗봇을 시나리오 단독 → 시나리오를 가드레일로 쓰는 RAG로 전환
- 기획·설계 문서 작성: `docs/00~11` (요구사항 / 아키텍처 / 로드맵 / 평가 / 정책 / 가드레일 / CRAG / 시퀀스 / 개발환경)
- DB 스키마 설계: `db/schema.sql` — Postgres+pgvector 하나에 스키마 7개 (struct/rag/meta/scen/guard/evalh/ops)
- 가드레일 = Kanana Safeguard 3종 채택 (Siren `I2` = 우리 TIER-A와 1:1, Apache-2.0 온프렘)
- CRAG 하네스 설계 — 기준 C1~C7 DB 저장, 실패유형→액션 매핑, 재검색 1라운드, 웹검색 폴백 제거
- Docker 스택 구성: `db` + `app` 2개 (+ `guardrail` GPU profile, 평소 안 뜸). compose 검증 통과
- 코드 0줄. 아직 `docker compose up` 안 함
- 다음: STEP 1 데이터 정리 → DB 기동·스키마 검증 → 합성 시드(struct/rag/scen) → 골든셋 200문항
- 확인 필요: 시나리오 트리 데이터 수령 가능? / 외부 LLM API 병원 보안정책상 허용?
- 미커밋

## 2026-08-25
- 깃 연습(복습) 커밋
- `프로젝트개요.md` 작성 (목표 3개)
- 신규 프로젝트 `AI_hub/office_automation` 생성 — 사무업무 자동화 (자동화 후보 6축 정리)
- `office_automation/meeting_minutes` 1차 개발 완료 — 녹음→로컬STT→Claude 구조화추출→md/html/json→구글드라이브
  - 렌더 파이프라인 스모크 테스트 통과. STT·드라이브는 패키지 설치 후 실제 검증 필요
- 서비스팀장님 발표덱 2부 분석 → `office_automation/docs/하네스_엔지니어링_플로우.md` + 원문폴더 `정리본.md`
- 덱 반영해 meeting_minutes 개선: 멈춤 게이트(D/A/Q 번호 → --accept), 미확인/미조사 구분, 덮어쓰기 방지
- meeting_minutes 자체검수(덱 원칙을 기준으로) → 결함 12개 확인·수정, 회귀 테스트 `tests/test_offline.py` 추가 (62 PASS)
  - 최대 결함: 스키마에 넣은 미확인/미조사가 최종 md/html 에 전파 안 됨 (원칙02 파장 위반)
- 서비스팀장님 JSX 뷰어 스크립트 업로드 — `서비스팀장님_자동화,하네스/jsx_viewer/` (원본 보관 + 파일연결 등록·동작 검증)
  - 검증 중 한계 3종 확인: 여러 줄 import / 화살표 export / 로컬 컴포넌트 import 는 깨짐 (재현 파일로 남김)
- 덱 1·2부 영역별 `정리본.md` (원문 대조로 누락 5건 보강)
- meeting_minutes 를 **API 키 없이 CLI 경로로 전환** — 추출을 `claude -p` 가 담당, 셀이 검증·저장
  - 실패 2회에서 나온 설계: 파일 쓰기 권한 막힘 → stdout 수신 / "JSON만 출력"이 안 먹힘 → `--append-system-prompt` 로 형식 강제
- 구글 드라이브: Drive for desktop 이 이미 동기화 중 → **OAuth 불필요**, `--sync`(동기화 폴더 복사) 추가. API 방식은 선택지로 유지
- GPU 확인: **AMD RX 7600 → faster-whisper 사용 불가**(CTranslate2 는 CUDA 전용). CPU 고정 + 장치 판정 근거·진행바 표시 추가
- **STT 실제 성공** — `mom_test1.m4a` → 57줄 1545자 (첫 실제 종단 검증)
- 사고: CLI 가 지정 파일 대신 옆 파일을 읽음(가족 통화 → 병원 테스트 파일). 겉보기 일관돼 더 위험
  - 대응 = **인용 검증 가드** (모든 quote 가 우리 녹취록에 있는지 대조 후 실패 시 중단) + 프롬프트에 입력 고정 명시
  - CLI 를 API 처럼 쓰는 stdin 방식은 셀 상단에 주석으로만 기록 (트레이드오프 포함)
- 노트북 2개: `setup.ipynb`(환경 준비 1회) / `run.ipynb`(실제 사용). 회귀 테스트 66 PASS
- 노션: 커넥터 전부 미인증(0 connected) → 셀은 준비 완료, **인증만 남음**
- 반복된 교훈 하나: **우리가 아는 값은 모델에게 맡기지 않는다** (제목·날짜는 셀이 덮어쓰고, 인용은 셀이 대조)

## 2026-08-26
- `.env` 를 사용자 통제 «단일 지점» 으로 정리 — 8섹션 29키, 전부 주석. 감사로 미연결 0건 확인
- 노트북 run.ipynb 정리: 셀별 «하는 일» 명시, 번호 재정렬(1~8), 목차 표 추가
- 드라이브 연동 완료 — OAuth 데스크톱 클라이언트 + `token.json`. 실제 업로드·경로 리포트 검증
  - 막힌 것: gcloud 로 OAuth 클라이언트 생성 불가(Console UI 전용), ADC 는 Google 이 Drive 스코프 거부
  - 사고: gcloud 프로젝트 ID 에 접미사가 붙어 «Drive API 안 켜진 다른 프로젝트» 로 갈렸음
- 업로드 전 «어디에 올라가나» 확인 셀 추가 (계정·경로·준비상태). 통과해야 전송
- 노션 커넥터 인증 완료 (`ada00004@gmail.com` 개인 워크스페이스). 헤드리스에서도 도구 붙음
- Playwright MCP 등록 (브라우저 조작). Google 로그인은 여전히 불가 — 자동화 탐지
- `AI_hub/CLAUDE.md` 신설 (하네스 3층) + `docs/기능정리.md` (MCP·Skill·인증 위치 한눈에)
- 발견: 전역 훅 11개가 `conhost.exe` 빈 명령으로 등록돼 있음 — 무동작. 확인 필요
- 고친 결함: `%autoreload` 가 dataclass 갱신 실패 → `%aimport -src.config` 로 제외
  / 입력 비었을 때 `Path('')` → `.` 폴더를 읽어 PermissionError → 검증 추가

- 모든 업로드에 «업로드 시각» 부착 (노션 제목 `(업로드 YYYY-MM-DD HH:MM)`, 드라이브 하위폴더)
- `src/pipeline.py` 를 노트북과 같은 함수 구조로 재작성 (`--auto` 오토모드 · `--check` 추가)
- `src/gui.py` + `회의록_GUI.bat` 신설 — 사람검수 모드 / 오토 모드 (tkinter, 추가 설치 없음)
- GUI 는 pipeline 단계 함수를 그대로 부름 (로직 중복 금지). 작업은 별 스레드 → 창 안 얼음
- 런처 파이썬은 `.env` 의 `PYTHON=` 이 정본 (노트북 커널과 같은 인터프리터)
- 오프라인 테스트 79개 통과 (`_resolve_device` 3-값 변경 반영 + 업로드시각·GUI 라벨 불변식 추가)
